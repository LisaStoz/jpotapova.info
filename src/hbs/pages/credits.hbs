---
title: <%= credits.title %>
intro: <%= credits.intro %>
class: <%= credits.class %>
---

{{#with credits}}

  {{#if tools}}
  <article>
    <ul>
      {{#each tools}}
      <li>{{{.}}}</li>
      {{/each}}
    </ul>
  </article>
  {{/if}}


{{/with}}
