---
title: <%= education.title %>
intro: <%= education.intro %>
---
{{#with education}}

  {{#each records}}
  <div>

    {{#if title}}
    <h3>{{title}}</h3>
    {{/if}}

  </div>
  {{/each}}

{{/with}}
